import { Typography } from "@mui/material";

const Logo = () => {
    return (
        <Typography
            variant="h4"
            component="h4"
            style={{
                textAlign: "center",
                padding: '1rem',
            }}
        >A.D.I.A.</Typography>
    )
};

export default Logo;