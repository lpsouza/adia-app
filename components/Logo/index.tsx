import { Typography } from "@mui/material";

const Logo = () => {
    return (
        <Typography
            variant="h4"
            component="h4"
            style={{
                textAlign: "center",
                padding: '1rem',
            }}
        >Sparck App</Typography>
    )
};

export default Logo;